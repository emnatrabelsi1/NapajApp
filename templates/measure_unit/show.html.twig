{% extends 'base.html.twig' %}

{% block title %}Unité{% endblock %}

{% block body %}
    <h1>Unité</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ measure_unit.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ measure_unit.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_measure_unit_index') }}">Liste des unités</a>

    <a href="{{ path('app_measure_unit_edit', {'id': measure_unit.id}) }}"><i class="fa-solid fa-pen"></i></a>

    {{ include('measure_unit/_delete_form.html.twig') }}
{% endblock %}
