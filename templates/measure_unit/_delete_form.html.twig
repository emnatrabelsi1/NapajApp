<form method="post" action="{{ path('app_measure_unit_delete', {'id': measure_unit.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ measure_unit.id) }}">
    <br/> <button class="btn btn-danger pull-right">Supprimer</button>
</form>
