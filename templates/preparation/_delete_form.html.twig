<form method="post" action="{{ path('app_preparation_delete', {'id': preparation.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ preparation.id) }}">
    <br/> <button class="btn btn-danger pull-right">Supprimer</button>
</form>
