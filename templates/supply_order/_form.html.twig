{% import "form_macro.html.twig" as form_macro %}

{{ form_start(form, {'autocomplete': 'off'}) }}

<div class="row">
    <div class="col-md-3">{{ form_row(form.supplier) }}</div>
    <div class="col-md-2">{{ form_row(form.amount) }}</div>
    <div class="col-md-5">{{ form_row(form.comment) }}</div>
</div>
<div class="display-none hidden">{{ form_row(form.created_at) }}</div>


<div class="col-md-5{% if not is_granted('ROLE_ADMIN') %} display-none hidden {% endif %}">{{ form_row(form.state) }}</div>


<button class="btn btn-success pull-right">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
