{% extends 'base.html.twig' %}

{% block title %}Ingredient{% endblock %}

{% block body %}
    <h1>Ingredient</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ingredient.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ ingredient.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ingredient_index') }}"><i class="fa-solid fa-arrow-left"></i> Retour à la liste</a>

    {{ include('ingredient/_delete_form.html.twig') }}
    <a class="pull-right" href="{{ path('app_ingredient_edit', {'id': ingredient.id}) }}"><button class="btn btn-tertiary" type="button">Modifier</button></a> 

{% endblock %}
