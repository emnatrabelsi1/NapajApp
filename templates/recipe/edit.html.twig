{% extends 'base.html.twig' %}

{% block title %}Modifier une recette{% endblock %}

{% block body %}
    <h1>Modifier la recette : {{ recipe.name }}</h1>
    
    {{ include('recipe/_form.html.twig', {'button_label': 'Valider'}) }}
    <br/>
    {{ include('recipe/_delete_form.html.twig') }}
    <a href="{{ path('app_recipe_index') }}"><i class="fa-solid fa-arrow-left"></i> Liste des recettes</a>

{% endblock %}
