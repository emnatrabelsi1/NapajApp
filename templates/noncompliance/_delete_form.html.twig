<form method="post" action="{{ path('app_noncompliance_delete', {'id': noncompliance.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ noncompliance.id) }}">
    <br/> <button class="btn btn-danger pull-right">Supprimer</button>
</form>
