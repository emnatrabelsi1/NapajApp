{% extends 'base.html.twig' %}

{% block title %}Catégories d'ingrédient{% endblock %}

{% block body %}
    <h1>Catégories d'ingrédient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ ingredient_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ingredient_category_index') }}"><i class="fa-solid fa-arrow-left"></i> Retour à la liste</a>

    <a href="{{ path('app_ingredient_category_edit', {'id': ingredient_category.id}) }}">edit</a>

    {{ include('ingredient_category/_delete_form.html.twig') }}
{% endblock %}
