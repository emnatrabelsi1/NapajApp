<form method="post" action="{{ path('app_ingredient_category_delete', {'id': ingredient_category.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ingredient_category.id) }}">
    <br/> <button class="btn btn-danger pull-right">Supprimer</button>
</form>
